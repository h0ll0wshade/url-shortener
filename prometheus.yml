global:
  scrape_interval: 15s        # how often to scrape
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'url-shortener'
    static_configs:
      - targets: ['host.docker.internal:8080']   # your Go app